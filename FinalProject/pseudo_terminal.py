import math as mt
import random as rdm
import time as tm
import misc_utils as misc
import ui
import events as ev
import tasks as tsk
import alerts as als
import winsound as ws
import tkinter as tk
from tkinter import ttk

def create_terminal(geometry, resizeable, title):
    print(f"new terminal created with title: {title}, geometry: {geometry}, and a resizeable condition of: {resizeable}")
def terminal_logic():
    print("terminal logic ran once")